import {Elements} from './mobx-component';

export default Elements({
  container: '.container',
  tabs: '.tabs',
  captureArea: '.captureArea',
  imageArea: '.imageArea',
  imagesArea: '.images-area',
  resultArea: '.resultArea'
});
